const express = require('express');
const router = express.Router();

// Note: Topics and Outcomes routes are registered in topicRoutes.js and outcomeRoutes.js respectively.

module.exports = router;

